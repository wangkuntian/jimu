apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.db.existingSecret }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "jimu.labels" . | nindent 4 }}
type: Opaque
stringData:
  db_password: "REPLACE_WITH_ACTUAL_PASSWORD"
  # base64(email:password) of OpenObserve admin; make secrets 生成
  zo_auth_token: "REPLACE_WITH_ACTUAL_BASE64_TOKEN"
  redis_password: "REPLACE_WITH_ACTUAL_PASSWORD"
  jwt_secret: "REPLACE_WITH_AT_LEAST_32_CHARACTERS_RANDOM"
