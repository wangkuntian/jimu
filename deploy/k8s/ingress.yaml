apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jimu-server
  namespace: jimu
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/rate-limit-window: "1m"
spec:
  ingressClassName: nginx
  rules:
    - host: api.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jimu-server
                port:
                  number: 80
  tls:
    - hosts:
        - api.example.com
      # TLS 证书 secret 需先创建（kubectl create secret tls jimu-tls --cert=tls.crt --key=tls.key）
      secretName: jimu-tls
