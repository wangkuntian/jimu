apiVersion: v1
kind: Secret
metadata:
  name: jimu-secrets
  namespace: jimu
type: Opaque
stringData:
  # 仅这两个环境变量被配置系统读取（覆盖 app.prod.yaml 中的值）
  MARIADB_PASSWORD: "change-me-in-production"
  JWT_SECRET: "change-me-use-at-least-32-characters"
