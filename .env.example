# Jimu 敏感配置
# 复制为 .env 并替换密码/密钥。
# 其他配置见 configs/app.yaml (开发) / configs/app.prod.yaml (生产)

# MariaDB root 密码
MARIADB_ROOT_PASSWORD=change-this-root-password
# 应用数据库密码（server 和 MariaDB 共用）
MARIADB_PASSWORD=change-this-jimu-db-password
# JWT 密钥（生产必须替换！）
JWT_SECRET=01234567890123456789012345678901
